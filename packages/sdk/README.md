# @solclaw/sdk

Shared TypeScript helpers for Solana + SolClaw API.

## Planned exports
- `createLaunchPayload` — validates fee split, uploads metadata
- `collectCreatorFee` — wraps PumpPortal `collectCreatorFee`
- `routeToFeeRouter` — deposits + distributes in one tx
- Types for proofs and API responses
